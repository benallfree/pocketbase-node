{
  "name": "pocketbase-node",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --dts --format cjs --out-dir dist",
    "watch": "tsup src/index.ts --dts --format cjs --out-dir dist --watch"
  },
  "devDependencies": {
    "pocodex": "link:pocodex",
    "tsup": "^8.2.4",
    "typescript": "^5.6.2",
    "@types/bun": "latest",
    "pocketbase-jsvm": "link:pocketbase-jsvm"
  }
}
